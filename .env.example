# --- Browser (Vite inlines VITE_* into the bundle; these are public by design) ---
VITE_SUPABASE_URL="https://enivasukccwjpvsgwcsd.supabase.co"
VITE_SUPABASE_ANON_KEY="<anon / publishable key>"

# --- Pages Functions only (never reaches the browser) ---
# Local: put these in .dev.vars (wrangler reads it). Production: `wrangler pages secret put`.
# SUPABASE_URL="https://<project-ref>.supabase.co"
# SUPABASE_SERVICE_ROLE_KEY="<service role key>"
